# V48.4 活动 update.zip 保护

GitHub Actions #84 失败于 `tests/v48SafeReconcile.test.js`。

现按仓库既有测试契约修复：只要根目录 `update.zip` 仍存在，`ci-reconcile-update-race-v052.js` 必须立即退出并输出“跳过 git reset”。

Apply Update ZIP 工作流本身会在测试和 APK 构建成功后才删除 update.zip，因此该保护能完整覆盖 npm install、npm test 和 Android build 阶段。
