# Build Stability Checklist

- Keep package and build configuration unchanged until migration is verified.
- Validate data loading before UI integration.
- Validate save compatibility before feature expansion.
- Keep modules independent.
