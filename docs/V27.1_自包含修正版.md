
# V27.1 自包含修正版

这份 update.zip 同时包含：
- package.json
- V27.1 一次性接入脚本
- runtime git push 重试脚本
- V27 回归测试
- target_home 全套拆分资源

修复 V27 首次误上传的问题：
此前上传的是“资源包”，没有接入脚本，导致仓库旧 pretest 找不到已删除脚本。

V27.1 会在应用成功后自动：
1. 删除 package.json 中的一次性 pretest；
2. 删除自身接入脚本；
3. runtime push 重试脚本也会删除自身；
4. 最终仓库不会留下会在后续普通提交中反复执行的失效 pretest。
