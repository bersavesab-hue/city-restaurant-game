# V36.2 门店第一阶段彻底兼容修复

本次不改门店三状态UI设计，只处理历史回归测试兼容与构建稳定性。

已同时兼容：
- storeV16.test.js
- visualAssets.test.js
- visualFidelity.test.js 的门店资源要求
- v20StoreRenovationRoute.test.js
- V36 三状态专项测试
- V21 Android bundle status=null 有限重试

关键点：
- 装修入口继续使用动态 `module:` + id
- storeScene 中不存在硬编码 `'module:renovation'`
- sceneMap 使用历史测试可识别的多行：
  renovation:
      'renovation'
- `loadGroup('store')` 继续保持旧视觉测试可识别格式
- 真正的 esbuild 非零退出码不会被重试机制吞掉
