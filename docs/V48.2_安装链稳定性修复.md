# V48.2 安装链稳定性修复

GitHub Actions #81 失败仍是测试误判。

修复内容：
- 删除点号/换行依赖的源码正则检测
- 字段改为直接语义名检测
- `preinstall` 只保留补丁应用与 JS 语法检查
- V48 自定义验证移动到 `npm run test:v48`
- 仓库正式 `npm test` 恢复为当前 0.5.2 的 `run-ci-tests-v052.js`

这样 npm install 不再因为源码格式测试而被阻断。
