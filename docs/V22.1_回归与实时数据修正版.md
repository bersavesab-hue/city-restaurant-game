# V22.1 回归与实时数据修正版

本版保留 V22 主页一对一布局改造。

修复：
1. `v22Package.test.js` 不再读取执行后会自删除的补丁脚本，改为验证最终 `src/main.js`。
2. 商圈详情“需求”继续使用 `demandSystem.getTotalDemand(district.id)` 实时值。
3. 底部导航恢复真实状态判断：
   - 城市 / 客流互斥；
   - 门店包含选址、设备、证照、员工、装修等子页面；
   - 其他页面按真实 scene id 高亮。
4. 保留 V20/V21 全部回归与 Android esbuild 构建检查。
