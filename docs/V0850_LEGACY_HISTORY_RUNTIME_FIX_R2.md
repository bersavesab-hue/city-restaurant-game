# V0.8.50 真机运行时修复 R2

第一次 V0.8.50 补丁的运行时修复逻辑与新增回归测试均通过，
但 CI 最终被 `tests/updateInfrastructureV0849.test.js` 拦截。

该历史测试把版本号严格写死为 `0.8.49`，因此任何 0.8.50 及以后版本都会失败。

R2 同时完成：

- 旧存档 interrupted 营业历史兼容修复；
- 连续经营趋势过滤不完整历史；
- 旧存档真实回归测试；
- 将 V0.8.49 基础设施测试改成“版本 >= 0.8.49”；
- 仍要求 Android versionName 与 package.json 完全一致；
- 安卓错误框输出真实异常信息。

基线仍然是 main 当前的 0.8.49，可直接替换失败的 update.zip。
