# V20.5 Android JS 重复声明修正版

本版不调整主页视觉与经营功能。

修复内容：
1. 修复 `handleTap()` 导航补丁中重复插入 `const scene` 的问题。
2. 原因是 `replaceBetween()` 会保留结束标记，而旧补丁 replacement 又重复写入同一结束标记。
3. 新增 `tests/v20MainSyntax.test.js`：
   - 检查不存在连续重复 `const scene` 声明；
   - 对补丁后的 `src/main.js` 执行 `node --check`。
4. 保留 V20.1~V20.4 所有回归测试和主页功能。

目标：全部测试通过后继续进入 esbuild Android JavaScript 构建。
