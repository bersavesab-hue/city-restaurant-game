# V36.1 兼容测试修复

V36 #52 失败原因不是门店逻辑，而是旧 `visualAssets.test.js`
对 `loadGroup('store')` 的换行格式做了字符串匹配。

本修复：
- 恢复旧测试期望的多行资源加载调用形式
- 保留 V36 无门店 / 筹备中 / 营业中三状态门店页
- V21 Android bundle 回归测试仅在 `spawnSync status=null`
  时最多重试 3 次
- 真正的非零 esbuild 退出码仍会立即报错，不掩盖真实构建失败
