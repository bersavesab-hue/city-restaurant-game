# V48.2 运行时集成测试修复

GitHub Actions #81 失败仍然来自测试转义，不是功能代码。

本版完全取消对 `shapeName/widthM/depthM/diningSlots/obstacles/entrances` 的源码正则扫描，改为直接 `require(floorGeometrySystem)`，调用 `getFloorGeometry()` 后检查真实返回对象。

同时对 7 种房型做运行时映射测试，避免再受换行、格式化和代码风格影响。
