# V20.3 动态商圈徽记测试修复

本包完整保留 V20.2 首页目标图重构内容，只修复 Apply Update ZIP #26 的最后一个测试阻塞点。

## 修复内容

- 将 `uiV20.test.js` 对 `district.demandDeltaRatio` 等字段的单行字符串硬匹配，改为允许正常换行/缩进的语义正则检查。
- 新增 `populationDelta` 检查，确保动态徽记真实读取人口变化。
- 强制检查 `竞争激烈 / 需求↑ / 租金低 / 人气高 / 需求↓` 五类动态状态仍存在。
- 新增 `v20DynamicDistrictBadge.test.js` 回归测试，锁定 `getDistrictVisualMeta()` 必须继续读取需求变化、饱和度、租金、人口变化和真实我的门店数量。
- 不删除、不绕过任何核心测试；V20.2 首页、装修归门店、客流热力和商圈边界修复全部保留。

## 对应故障

Apply Update ZIP #26:
`AssertionError: 商圈标签必须由真实动态数据决定`

根因是测试要求字段必须写成连续字符串 `district.demandDeltaRatio`，而新版实现采用合法换行：
`district` + 换行 + `.demandDeltaRatio`。
