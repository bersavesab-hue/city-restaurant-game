# V37.1 门店定稿完整兼容修复

本版不改变用户已经定稿的四种门店页面结构。

修复：
- 恢复真实 `getRenovationProgress(shopId)`，并由筹备页实际调用
- 保留 V36 历史快捷入口契约，但不改变 V37 页面可见文案
- 独立“游戏底层自动测试”没有 npm install 时：
  `v21AndroidBundle.test.js` 自动退化为 Android 入口 JS 语法检查
- 正式 Apply Update ZIP 有 esbuild 时仍进行真实 bundle 测试
- 真正的 esbuild 非零退出码仍然失败
- V16 / V18 / V20 / V36 / V37 门店契约统一加入完整兼容检查
