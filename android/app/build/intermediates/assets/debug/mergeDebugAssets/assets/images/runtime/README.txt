V10 runtime image assets

These files were cropped/compressed from images that had already been generated.
No new art generation was used.

Loading:
- storeScene -> visualAssetSystem.loadGroup('store')
- districtScene -> visualAssetSystem.loadGroup('district')
- renovationScene -> visualAssetSystem.loadGroup('renovation')

Large design mockups under assets/images/generated/ are NOT runtime assets and are removed from the APK before packaging.
